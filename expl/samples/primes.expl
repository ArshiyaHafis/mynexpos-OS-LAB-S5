int main()
{
	decl
		int i, temp, a, j, f, b;
	enddecl
	
	begin
		i=2;
		while(i<=100) do
			a=i/2;
			j=2;
			f=0;
			while(j<a) do
				b=i%j;
				if(b==0) then
					f=1;
					break;
				endif;
				j=j+1;
			endwhile;
			if (f==0) then
				temp = exposcall("Write", -2, i);
			endif;
			i=i+1;
		endwhile;
		return 0;
	end
}
